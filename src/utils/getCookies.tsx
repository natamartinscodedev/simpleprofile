// import { cookies } from 'next/headers'

// export default function GetCookiesProfile(setData: any) {
//   const cookieStore = cookies()
//   const data = cookieStore.get('shared_profile')
//   const sharedProfileData = data ? JSON.parse(data.value) : null

//   setData(sharedProfileData)
//   console.log('Value Cooklies ==>', sharedProfileData)
// }
